START

User logs in
SELECT view attendance option
DETERMINE user role

IF user role = Student THEN
    RETRIEVE student's own attendance record
    DISPLAY attendance
ELSE IF user role = Lecturer OR Admin THEN
    SELECT course and class
    RETRIEVE attendance records
    DISPLAY attendance
ENDIF

END
